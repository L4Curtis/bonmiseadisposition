import { ChartCard } from '@/components/dashboard/ChartCard';

/** Placeholder — sera remplacé par le lot 4b (GET /kpi/delais). */
export function DelaisTab() {
  return (
    <ChartCard title="Workflow et délais" empty emptyMessage="Bientôt disponible." />
  );
}
