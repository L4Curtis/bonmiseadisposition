import { ChartCard } from '@/components/dashboard/ChartCard';

/** Placeholder — sera remplacé par le lot 4a (GET /kpi/parc). */
export function ParcTab() {
  return (
    <ChartCard title="Parc et prêts" empty emptyMessage="Bientôt disponible." />
  );
}
