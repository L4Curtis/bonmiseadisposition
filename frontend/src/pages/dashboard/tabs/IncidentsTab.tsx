import { ChartCard } from '@/components/dashboard/ChartCard';

/** Placeholder — sera remplacé par le lot 4c (GET /kpi/incidents). */
export function IncidentsTab() {
  return (
    <ChartCard title="Qualité et incidents" empty emptyMessage="Bientôt disponible." />
  );
}
