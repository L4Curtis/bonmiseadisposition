{
  "name": "bmad-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Tests de bout en bout (Playwright) — bons de mise à disposition",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "@types/node": "^22.20.0",
    "typescript": "^5.9.0"
  }
}
